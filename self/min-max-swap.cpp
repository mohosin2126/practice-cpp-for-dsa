#include <bits/stdc++.h>
using namespace std;

int main()
{
    int x = 10;
    int y = 20;
    cout << max(x, y) << endl;
    cout << min(x, y) << endl;
    swap(x, y);
    cout << x << " " << y;
    return 0;
}
